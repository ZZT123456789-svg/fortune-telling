# 道问 Design System v1.0

适用范围：首页、功能主页、功能弹窗、排盘结果、AI 解读、书库、独立支付页及联系面板。  
基准：桌面端 1440px、移动端 390px；移动端优先验证 360–430px。

参考对象：[青囊 · Aether Pouch](https://www.qingnang.cc/) 的产品表达与信息组织。只借鉴其“古籍为根、AI 参详、专业克制、结果可溯源”的体系化思路，不复制品牌资产、文案、布局或登录方案。道问继续保留游客身份和服务端数据绑定，不因参考 `/auth` 页面重新引入前台登录系统。

## 1. 产品判断

### 产品气质

| 判断 | 具体规则 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| 核心气质 | 东方、静谧、可信、克制、略带仪式感；强调“研究工具”而非“神秘娱乐” | 首页、排盘、古籍阅读、支付 | 不做庙宇仿古网页、游戏化玄幻 UI 或赛博霓虹 |
| 品牌关键词 | 「洞察」「秩序」「推演」「自省」 | 标题、动效、文案、插画 | 不使用“逆天改命、百分百准确、必然发财”等承诺 |
| 现代性 | 传统文化外观，现代工具体验；参考青囊把术数分组、证据来源和推演深度做成清晰产品层级 | 表单、导航、结果、付费流程 | 不用复杂仿古控件牺牲可读性和操作效率 |
| 高级感来源 | 大留白、明确层级、材质克制、动效缓慢稳定 | Hero、核心卡片、结果摘要 | 不依靠大量金色、强发光、连续动画制造“贵” |

### 目标用户

1. **大众体验用户（主用户）**：20–45 岁，移动端进入，先体验八字/紫微/每日签运，再决定是否购买 AI 解读。
2. **重复使用用户**：关注余额、兑换码、历史输入恢复和支付返回，不愿被登录打断。
3. **专业或半专业用户**：需要完整十神、藏干、旺衰依据、大运流年、紫微全星曜等高密度信息。
4. **古籍阅读用户**：长时间阅读，对字号、行距、目录、阅读进度和纸张模式敏感。

青囊参考给出的关键产品启发：功能不只按“工具名”罗列，而应按用户意图分为“命（长期结构）/ 卦（具体问事）/ 日（日常节律）/ 书（知识溯源）”；AI 解读应支持“专业/通俗”深度切换，但不建议照搬其“学者/隐士”命名和文案。

### 核心使用场景

| 场景 | 主要目标 | 设计优先级 |
|---|---|---|
| 首页进入 | 建立品牌氛围，快速进入功能 | 背景清晰、真实控件、首屏 CTA 可见 |
| 快速排盘 | 尽量少填错，快速得到基础结果 | 表单清晰、默认值合理、错误就地提示 |
| 深度阅读结果 | 理解结论和依据 | 结构化层级、导航吸顶、长内容可扫读 |
| AI 解读 | 在不重算命盘的前提下解释结构化结果 | 清晰区分用户问题、结论、依据和提示 |
| 购买积分 | 安全、可预期、可返回 | 独立页面、状态明确、返回后恢复输入 |
| 古籍阅读 | 持续阅读而不疲劳 | 阅读宽度、字号、目录和进度优先 |
| 来源核验 | 从结论回到对应古籍条目 | 引文卡、出处链接、原文上下文 |

## 2. 设计原则

| 原则 | 规范 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| 内容先于装饰 | 背景只承担氛围，文字和操作始终保持足够对比 | 全站 | 不让漩涡、雾气或金光穿过正文和输入区 |
| 一屏一个主任务 | 每个页面/弹窗只保留一个最高优先级 CTA | 表单、支付、AI 解读 | 不在同一区域放两个同权重金色实心按钮 |
| 渐进披露 | 新手先看基础结果，专业信息通过 Tab/折叠展开 | 八字、紫微、AI 结果 | 不把所有表格和解释堆成一条超长页面 |
| 可溯源而非堆引文 | 每条核心判断可展开“依据”和“出处”；原文是证据层，不是装饰层 | AI 解读、专业结果、书库 | 不在每段后机械附一串书名，不引用无法核验的原文 |
| 仪式感不阻塞 | 首次进入可有镜头推进；重复访问直接进入或可跳过 | 首页入场 | 不让动画阻挡支付、表单输入或返回操作 |
| 状态可确认 | 点击、加载、成功、失败、余额变化都有视觉反馈 | AI、兑换码、支付 | 不只用颜色表达状态，不静默失败 |
| 移动端是真正全屏工具 | 功能弹窗在 ≤760px 铺满视口，内部滚动、底层锁定 | 所有功能弹窗 | 不使用居中小弹窗，不出现双滚动条 |
| 三主题同语义 | 夜晚/白天/中午只替换材质和颜色，不改变组件层级 | 全站主题切换 | 不给每个主题另写一套结构 CSS |

## 3. 视觉语言

### 色彩系统

#### 语义层级

| 层级 | 夜晚模式 | 白天模式 | 中午模式 | 适用场景 | 不要怎么用 |
|---|---|---|---|---|---|
| Canvas | `#070808` | `#ECE6D8` | `#6A471F` | 页面最底层和背景图兜底 | 不直接作为卡片背景 |
| Surface | `rgba(16,18,17,.82)` | `rgba(255,252,245,.76)` | `rgba(66,45,20,.64)` | 导航、卡片、弹窗毛玻璃 | 不连续叠加超过两层半透明面板 |
| Surface strong | `rgba(9,10,10,.96)` | `rgba(250,246,235,.94)` | `rgba(42,29,13,.88)` | 输入、支付、阅读器、长文本区域 | 不透明度低于 .82 承载长正文 |
| Primary text | `#F2EDE3` | `#2A2218` | `#FFF6DF` | 标题、正文、金额 | 不使用纯白 `#FFF` 或纯黑 `#000` 大面积正文 |
| Secondary text | `#D4CBBB` | `#5C4E3A` | `#E7D2A5` | 描述、次级结果 | 正文对比度不得低于 4.5:1 |
| Muted text | `#AAA294` | `#746852` | `#C3AD82` | 时间、标签、辅助信息 | 不用于关键说明、价格、错误信息 |
| Brand gold | `#D3A653` | `#8B6F3A` | `#F2C76C` | 激活态、品牌字印、重点数据 | 单屏金色高亮面积不超过可视面积 12% |
| Gold bright | `#F2D38B` | `#A78348` | `#FFE2A0` | 主按钮渐变和焦点 | 不作为大段正文颜色 |
| Divider | `rgba(221,180,93,.26)` | `rgba(74,58,30,.18)` | `rgba(255,218,143,.28)` | 边框、分隔线 | 不使用不透明金色描边包围所有元素 |

#### 状态色

| 状态 | 色值 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| Success | `#68B98A` | 支付到账、兑换成功、复制完成 | 不只显示绿色，必须同时有文字或图标 |
| Warning | `#DDB25D` | 日期歧义、输入提醒、余额不足 | 不用于普通装饰 |
| Danger | `#D9796D` | 接口失败、无效字段、订单失败 | 不用高饱和纯红大面积铺底 |
| Info | `#77A8C5` | 规则说明、处理中、帮助信息 | 不与可点击链接混为同一种样式 |
| WeChat | `#36D267` | 仅联系面板微信字段及复制按钮 | 不扩散到导航或主按钮 |
| QQ | `#36A9FF` | 仅联系面板 QQ 字段及复制按钮 | 不扩散到全站信息色 |

### 字体系统

| 角色 | 字体栈 | 尺寸/行高 | 适用场景 | 不要怎么用 |
|---|---|---|---|---|
| Display | `"Noto Serif SC", "Songti SC", STSong, serif` | Hero 96–160/0.96；移动 72–112 | “道问”、页面大标题、支付金额 | 不用于表单和长正文 |
| Heading | 同 Display | H1 40/1.25，H2 28/1.35，H3 20/1.4 | 页面/模块/卡片标题 | 不低于 18px 承担章节标题 |
| Body | `"Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif` | 默认 14/1.7；结果与 AI 16/1.8 | 表单、说明、结果、按钮 | 不用楷体承载长文本 |
| Classical | `KaiTi, STKaiti, "Noto Serif SC", serif` | 18–20/1.9 | 古籍原文、短句题签 | 不用于支付、错误、隐私说明 |
| Numeric | `Inter, "DIN Alternate", system-ui, sans-serif` | 继承组件字号；tabular nums | 金额、余额、日期、分数 | 不混用手写体数字 |

补充规则：正文最小 14px；移动端关键说明最小 15px；辅助信息最小 12px。当前 `.61rem–.72rem` 的导航、标签和支付说明应提升到 12–13px。

### 间距与栅格

| 项目 | 规范 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| 基础单位 | 4px；主要节奏使用 8px 倍数 | 全站 | 不出现 13、17、23px 等无语义间距 |
| 页面容器 | 最大 1180px；结果/表单最大 960px；阅读正文 720px | 桌面端 | 不让长文本横跨 1000px 以上 |
| 页面边距 | ≥1200px: 32px；768–1199: 24px；≤767: 16px | 所有页面 | 手机端不低于 12px，除全屏弹窗边框 |
| 栅格 | 桌面 12 列/24px gutter；平板 8 列/20px；手机 4 列/12px | 卡片、支付套餐、结果布局 | 不靠固定像素宽度拼列 |
| 垂直节奏 | 小节 24px；模块 48px；大区块 72px | 功能主页、结果页 | 不用连续空白撑高弹窗 |
| 点击热区 | 桌面 ≥40px；移动 ≥44px | 按钮、关闭、Tab、导航 | 不因图标只有 20px 就把点击区也做成 20px |

建议间距刻度：`0, 4, 8, 12, 16, 24, 32, 48, 64, 80, 96`。

### 圆角、边框与阴影

| 项目 | 规范 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| `radius-sm` | 8px | 标签、小按钮、输入内部元素 | 不用于页面级容器 |
| `radius-md` | 12px | 输入、按钮、信息卡、联系行 | 不混入 9/10/11/13/14px |
| `radius-lg` | 16px | 功能卡、结果卡、支付套餐 | 不同同级卡片不得有不同圆角 |
| `radius-xl` | 24px | 桌面弹窗、支付主容器 | 移动全屏弹窗不得保留四周圆角 |
| Pill | `999px` | 导航选中态、主题切换、短标签 | 不用于长段按钮或表单容器 |
| 默认边框 | 1px `divider` | 卡片、输入、弹窗 | 不使用双层粗金框 |
| 字印边框 | 2px `brand-gold` | 功能字印、标题字印 | 不用于普通图标按钮 |
| Elevation 1 | `0 8px 24px rgba(0,0,0,.20)` | 导航、卡片 hover | 不给每张静态卡片强阴影 |
| Elevation 2 | `0 24px 70px rgba(0,0,0,.46)` | 弹窗、联系面板 | 不叠加多个不同方向阴影 |
| Glow | `0 0 20px rgba(211,166,83,.16)` | 激活字印、主 CTA | 不用于正文或整张卡片持续发光 |

## 4. 核心组件规范

| 组件 | 规范 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| 顶部导航 | 固定顶部；桌面 64px、移动 56px；黑/宣纸/暖金毛玻璃；品牌、主导航、账户操作三区 | 首页、功能主页、书库、支付页 | 不新增第二层悬浮工具条；移动端不使用汉堡菜单 |
| 移动导航 | 保持主入口横排，主导航区允许横向滚动；余额与充值为固定操作区；主题仅显示三个图标 | 360–760px | 不允许文字被截成半个字；不把所有按钮缩到 10px 字号 |
| 主题切换 | 日出/太阳/月亮三个单色 SVG 并排；当前态有底色、边框和 `aria-pressed` | 全站导航 | 不使用 Emoji；不刷新页面、不关闭弹窗 |
| Hero | 100dvh；背景图独立于 HTML 控件；中央景物裁切；标题和 CTA 居中 | 首页 | 不把图片里的旧导航/按钮当作真实控件 |
| Hero 主按钮 | 实色浅金/象牙渐变，最小 210×56；只用于“开始入局” | 首页 | 不给“每日签运”同样实心权重 |
| 次按钮 | 透明毛玻璃 + 1px 金边 | 每日签运、取消、次级动作 | 不使用低对比灰字 |
| 功能卡 | 桌面 4 列，平板 2 列，手机 1–2 列；字印、名称、描述、标签四层 | 功能主页 | 不用彩色系统 Emoji；不让描述低于 13px |
| 功能字印 | 桌面 52×52、移动 42×42；内部单字 20/18px；深黑底、2px 金边、轻微金光 | 八字、紫微、六爻等卡片 | 不同时使用字印和 Emoji 表示同一功能 |
| 主按钮 | 高 44px（移动 48px）；实色金色渐变；文字 14–16px/700 | 提交排盘、购买、确认兑换 | 不在 disabled 状态保持金色高亮 |
| 输入框 | 高 44px；12px 圆角；标签在上；错误信息在下；聚焦 3px 低透明金环 | 所有表单 | 不依赖 placeholder 代替标签；不使用浏览器原生彩色 spinner 破坏风格 |
| 桌面弹窗 | 宽度按内容 640/800/960 三档；最大高 `100dvh - 48px`；仅弹窗内部滚动 | 功能输入与结果 | 不固定 960px 后留下大面积空白；不嵌套弹窗 |
| 移动弹窗 | `100vw × 100dvh`；无侧边圆角；顶部 1px 金边；安全区适配；内部滚动 | ≤760px | 不居中小弹窗；底层页面不得滚动 |
| 关闭按钮 | 44×44 热区，单色 X；位置固定在弹窗标题栏右侧 | 所有弹窗/面板 | 不用字符位置漂移的 “×”；不与提交按钮重叠 |
| 结果 Tab | 42–44px 高、吸顶；激活项实色金；手机可横滑 | 八字、紫微结果 | 不把十几个章节一次全部展开 |
| 命盘卡 | 每柱竖向卡；标题、十神、天干、地支、藏干、辅助信息分层 | 八字专业模式 | 不退回一张四行超长表格 |
| 数据表格 | 表头吸顶；数值右对齐；手机放入横向滚动容器 | 紫微、十神、流年 | 不让整页随表格横向滚动 |
| AI 解读 | 正文 16px/1.85；分“结论/依据/影响/提醒”；每节独立卡片 | 深度解读 | 不使用 13px 灰字；不让 AI 重新推算四柱 |
| 书库卡片 | 书封、书名、作者/版本、简介、阅读进度、继续阅读 | 书库 | 不使用与功能卡不同的颜色/阴影体系 |
| 阅读器 | 正文宽 680–760px；原文 18–20px/1.9；目录桌面侧栏、手机抽屉 | 古籍阅读 | 不让正文占满屏宽；不强制楷体显示所有说明 |
| 支付页 | 必须独立页；套餐、创建订单、结果状态在同页；返回恢复来源功能和输入 | 充值/购买积分 | 不放入功能弹窗；不嵌套支付弹窗；不使用未经验证的安全承诺 |
| 联系面板 | 导航内按钮展开；只允许 X 关闭；微信绿、QQ 蓝仅用于字段和复制按钮 | 首页、书库、支付页 | 不额外悬浮一层；不点击外部自动关闭 |

### 4.1 八字排盘录入流程（参考青囊，转为道问体系）

青囊八字页的有效结构是：`称谓 → 性别 → 历法类型 → 出生日期时间 → 出生地点 → 高级选项 → 开始排盘`。道问采用同样的信息渐进顺序，但保持自己的黑金视觉、游客身份和现有成熟历法引擎。

| 步骤 | 道问规范 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| 1. 基础身份 | “称谓”设为可选；性别用等宽分段按钮显示“男 / 女”，专业模式补充“乾造 / 坤造” | 单人排盘 | 不要求真实姓名；不把乾坤术语作为唯一标签 |
| 2. 录入方式 | 使用“公历 / 农历 / 四柱”三段切换；默认公历；切换后只替换日期录入区 | 八字表单顶部 | 不让用户同时看到三套输入；切换不得清空已填写的称谓和性别 |
| 3. 日期时间 | 年月日时分按自然顺序；农历明确显示“正月”并按需显示“闰月”；时辰与精确时间可互相辅助 | 公历/农历排盘 | 不用 `1–12` 代替农历月份名称；不知道分钟时不强迫用户编造 |
| 4. 出生地点 | 省/市/区三级选择为默认；提供搜索与“海外/找不到”入口；展示采用的经度和时区 | 真太阳时校正 | 不在首屏展开经纬度输入；不静默使用错误地区 |
| 5. 高级选项 | 折叠区包含真太阳时、子初换日、早晚子时、时区/经度、历法说明 | 专业用户 | 不把关键默认值隐藏且不说明；不让高级选项改变四柱后没有提示 |
| 6. 提交前摘要 | 按钮上方用一行摘要显示“公历 2003-02-20 19:00 · 北京 · 真太阳时已开启” | 减少误填 | 不让用户提交后才发现历法或地点选错 |
| 7. 主操作 | 页面只保留一个主按钮：“生成命盘”；排盘本身免费时明确标注“基础排盘免费” | 表单末端 | 不在同区并列“排盘”“AI 解读”“充值”三个主按钮 |
| 8. 错误处理 | 错误贴近字段，自动滚动到第一个错误；保留全部输入；说明修正方法 | 日期无效、地区缺失、接口异常 | 不只弹通用 Alert；不清空用户数据 |

桌面布局：表单容器最大 760px；年月日最多三列，时间与地点另起一行。  
移动布局：单列；历法与性别保持横排分段按钮；所有输入宽度 100%，字段间距 16px。

### 4.2 排盘结果信息架构

1. **命盘摘要**：公历/农历、真太阳时、生肖、四柱和校正说明。
2. **四柱命盘**：年、月、日、时四张竖向卡，完整展示十神与藏干。
3. **结构判断**：旺衰、格局、调候、喜忌；每项包含“结论 + 依据”。
4. **关系网络**：合、冲、刑、害、破、三合、三会、六合。
5. **运势时间轴**：真实起运时间、大运、流年，先时间轴后逐步详解。
6. **古籍依据**：相关原文、书名、篇章和当前判断的对应关系。
7. **AI 深度解读**：读取上述结构化数据，作为最后一层，不重新计算四柱。

适用场景：完整八字结果和专业模式。  
不要怎么用：不要照搬青囊的品牌名、积分名、人格文案或登录档案入口；不要为了“像青囊”改变已经校验过的命理算法。

## 5. 状态与动效规范

| 状态 | 规范 | 适用场景 | 不要怎么用 |
|---|---|---|---|
| Hover | 上移 2px、边框增强、阴影提升一级；200ms | 桌面卡片/按钮 | 触屏设备不保留 hover 位移 |
| Pressed | `translateY(1px)`、亮度降低 6% | 可点击控件 | 不做大幅缩放或弹跳 |
| Focus visible | 2px 高对比外框 + 2px offset | 键盘导航 | 不用 `outline:none` 后没有替代方案 |
| Selected | 实色金底或高对比玻璃底，同时改变文字/图标 | Tab、主题、导航 | 不只靠边框细微变化 |
| Disabled | 透明度 .42，去阴影，禁用指针；保留可读标签 | 按钮、翻页、套餐 | 不隐藏 disabled 控件导致布局跳动 |
| Loading | 原位置锁定宽高；按钮显示旋转线环 + “处理中…” | AI、支付、兑换、排盘 | 不用无限全屏遮罩；不允许重复提交 |
| Success | 就地绿色图标 + 明确结果 + 下一步 | 支付到账、复制、兑换 | 不仅 toast 一闪而过 |
| Error | 字段级错误就地显示；系统错误给重试和返回 | 表单/API/支付 | 不只弹“请稍后重试”，必须保留用户输入 |
| Empty | 说明为什么为空 + 可执行动作 | 历史、搜索、结果模块 | 不显示空白大卡片 |
| Theme transition | 颜色、边框、背景 500ms；不刷新、不关闭弹窗 | 三主题切换 | 不对布局尺寸做主题动画 |
| 标准过渡 | 200ms `cubic-bezier(.2,.72,.25,1)` | Hover、展开、Tab | 不在一个组件混用 200/280/300/700ms |
| 入场动画 | 首次 Hero→功能页 850ms：向下推进、雾散、内容上浮；后续进入缩短 | “开始入局” | 不超过 1.2s；支付、表单和返回不播放 |
| Reduced motion | 移除位移、缩放、雾气和持续动画，仅保留淡入 | 系统减少动态设置 | 不忽略 `prefers-reduced-motion` |

## 6. 图标与插画规范

| 规则 | 适用场景 | 不要怎么用 |
|---|---|---|
| 通用 UI 图标统一使用一套单色 SVG，24×24 viewBox，1.75–2px 线宽，圆角端点 | 导航、复制、关闭、搜索、主题、返回 | 不使用 🔮、📖、⚠️、🔒、🌸、📅 等系统 Emoji |
| 功能图标优先采用“东方字印”：八、紫、梅、爻、签、历、书等 | 功能卡和弹窗标题 | 不同时混用写实插画、Emoji 和线性图标 |
| 状态图标必须配文字 | 成功、警告、错误、信息 | 不让颜色或图形成为唯一信息来源 |
| 夜晚背景保留山水、雾气和中央漩涡；白天使用淡雅宣纸水墨；中午使用暖金山水 | Hero 和页面底层 | 不在图片中预烘焙导航、标题和按钮 |
| 背景图片应保留中央安全区；桌面 `cover`，移动中心裁切，焦点约 50% 35% | 响应式 Hero | 不在移动端把漩涡裁到屏幕边缘 |
| 卡片内装饰纹理透明度 ≤8%，不穿过正文 | 功能卡、支付套餐 | 不用高频纹理造成文字发虚 |

## 7. 文案语气规范

| 规则 | 适用场景 | 不要怎么用 |
|---|---|---|
| 品牌语气：沉静、简洁、引导自省 | Hero、章节标题、AI 开场 | 不故作玄虚或堆砌古文 |
| 操作语气：直接描述动作 | “开始入局”“生成命盘”“购买 10 次”“返回八字” | 不使用“确认”“好的”这类缺少上下文的按钮 |
| 结果语气：结论 + 依据 + 条件 | 命理解释、AI 解读 | 不使用绝对判断，不显示“可信度”标签 |
| 风险语气：说明事实和下一步 | 支付失败、余额不足、日期歧义 | 不使用责备语气，不丢失用户输入 |
| 付费语气：透明、中性、可核对 | 价格、次数、到账状态 | 不使用虚假稀缺、倒计时、夸张“超值”诱导 |
| 专业边界：传统文化体验与自我观察参考 | 免责声明和敏感结果 | 不构成医疗、法律、投资或心理诊断 |

推荐模板：

- 成功：`已到账 10 次解读，可返回八字继续查看。`
- 处理中：`正在创建支付宝订单，请勿重复点击。`
- 可恢复错误：`订单暂未创建，已保留你的命盘信息。请重试或返回。`
- 余额不足：`当前剩余 0 次。购买后将返回本功能，并恢复已填写内容。`

## 8. 设计红线

1. **禁止 Emoji 作为正式 UI 图标。**仅用户生成内容可原样保留 Emoji。
2. **禁止嵌套弹窗。**支付永远是独立页面；联系面板不是模态弹窗。
3. **禁止底层与弹窗同时滚动。**桌面弹窗内部滚动，移动端全屏内部滚动。
4. **禁止硬编码主题色到组件。**组件只引用语义 token。
5. **禁止同级组件出现不同圆角、不同阴影和不同动效时长。**
6. **禁止低于 12px 的可见文字；AI/结果正文禁止低于 16px。**
7. **禁止使用纯黑大面板配低对比灰字。**必须通过 surface 和 text token 保证层级。
8. **禁止多个主按钮并列。**主任务只允许一个实色按钮，其余使用描边或文字按钮。
9. **禁止将山水背景、金色描边、发光、纹理同时开到最高强度。**每个区域最多两个装饰信号。
10. **禁止通过 CSS `!important` 继续堆叠修补。**迁移后组件样式必须由 token 和层级清晰的选择器控制。
11. **禁止内联样式定义颜色、字号、间距。**现有内联样式逐步迁移为组件类。
12. **禁止支付安全、到账或身份能力使用未经后端事实支持的文案。**

## 9. 当前实现的不一致与整改优先级

| 优先级 | 当前问题 | 截图/代码表现 | 建议 |
|---|---|---|---|
| P0 | 两套主题变量并存 | `style.css` 的浅色 `--bg-*` 与 `ui-polish.css` 的黑金 `--dw-*` 通过覆盖竞争 | 建立唯一语义 token，三个主题只改 token 值 |
| P0 | 字体可读性不稳定 | Body 以楷体为首选，AI/结果又局部改宋体或无衬线 | 标题宋体、正文无衬线、古籍原文楷体三层固定 |
| P0 | 正文对比不足 | 功能卡说明、弹窗标签、支付注释在截图中明显发灰 | 使用 secondary text，关键说明不得用 muted |
| P0 | 移动导航挤压 | 390px 截图中导航文字被截断，账户操作与导航争夺宽度 | 主导航内部横滑；账户区固定；设置最小点击宽度 |
| P1 | 图标语言混用 | 截图仍有水晶球、花、手掌、卷轴、日历等彩色 Emoji，另有字印体系 | 全部替换为单色 SVG 或统一字印 |
| P1 | 圆角体系过多 | 代码同时存在 5/6/8/9/10/11/12/13/14/16/18/20/22/24/31/999px | 收口为 8/12/16/24/pill 五档 |
| P1 | 弹窗尺寸不随内容 | 桌面梅花弹窗使用大容器，表单只占顶部，下面大量空白 | 弹窗使用 640/800/960 三档并按内容选择 |
| P1 | 移动表单宽度不足 | 紫微截图中输入框/选择器仅占内容宽度，视觉断裂且难点按 | 移动端表单控件 `width:100%`，单列 44–48px 高 |
| P1 | 支付页与主站体系分叉 | 支付页单独定义导航、卡片、状态和 Assurance 样式 | 复用统一 Nav/Card/Button/Status token，只保留独立布局 |
| P1 | 书库另建色彩体系 | `library.css` 维护独立 `--gold/--panel/--text` | 接入全站语义 token，阅读器只增加阅读专属 token |
| P2 | 动效时长分散 | 当前存在 180/200/250/280/300/550/700/850ms | 收口为 120/200/240/500/850ms 五档 |
| P2 | 样式覆盖过重 | 多个 CSS 文件和大量 `!important` 依赖加载顺序 | 重构为 tokens → base → components → pages → utilities |
| P2 | 三主题尚未形成同一系统 | 当前核心仍是夜晚黑金；白天截图属于另一套旧样式 | 新增 `data-theme`，只切 token 和背景资源，不切 DOM |

## 10. Design Tokens（CSS Variables）

```css
:root {
  color-scheme: dark;

  /* Typography */
  --font-display: "Noto Serif SC", "Songti SC", STSong, serif;
  --font-body: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
  --font-classic: KaiTi, STKaiti, "Noto Serif SC", serif;
  --font-numeric: Inter, "DIN Alternate", system-ui, sans-serif;
  --text-xs: 12px;
  --text-sm: 13px;
  --text-md: 14px;
  --text-lg: 16px;
  --text-xl: 20px;
  --text-2xl: 28px;
  --text-3xl: 40px;
  --leading-body: 1.7;
  --leading-reading: 1.9;

  /* Spacing */
  --space-0: 0;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;
  --space-20: 80px;
  --space-24: 96px;

  /* Geometry */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  --radius-pill: 999px;
  --control-h: 44px;
  --control-h-mobile: 48px;
  --nav-h: 64px;
  --nav-h-mobile: 56px;
  --container-page: 1180px;
  --container-content: 960px;
  --container-reading: 720px;

  /* Motion */
  --ease-standard: cubic-bezier(.2, .72, .25, 1);
  --duration-instant: 120ms;
  --duration-fast: 200ms;
  --duration-modal: 240ms;
  --duration-theme: 500ms;
  --duration-entry: 850ms;

  /* Semantic states */
  --color-success: #68b98a;
  --color-warning: #ddb25d;
  --color-danger: #d9796d;
  --color-info: #77a8c5;
  --color-wechat: #36d267;
  --color-qq: #36a9ff;

  /* Elevation */
  --shadow-1: 0 8px 24px rgb(0 0 0 / 20%);
  --shadow-2: 0 24px 70px rgb(0 0 0 / 46%);
  --shadow-modal: 0 35px 100px rgb(0 0 0 / 70%);
}

:root,
[data-theme="night"] {
  color-scheme: dark;
  --color-canvas: #070808;
  --color-surface: rgb(16 18 17 / 82%);
  --color-surface-soft: rgb(23 24 22 / 72%);
  --color-surface-strong: rgb(9 10 10 / 96%);
  --color-text: #f2ede3;
  --color-text-secondary: #d4cbbb;
  --color-text-muted: #aaa294;
  --color-brand: #d3a653;
  --color-brand-bright: #f2d38b;
  --color-brand-contrast: #17130c;
  --color-divider: rgb(221 180 93 / 26%);
  --color-divider-strong: rgb(235 195 115 / 50%);
  --color-focus: rgb(242 211 139 / 68%);
  --glass-blur: 18px;
  --image-hero: url("../assets/daowen-hero-bg.png");
}

[data-theme="day"] {
  color-scheme: light;
  --color-canvas: #ece6d8;
  --color-surface: rgb(255 252 245 / 76%);
  --color-surface-soft: rgb(248 242 230 / 68%);
  --color-surface-strong: rgb(250 246 235 / 94%);
  --color-text: #2a2218;
  --color-text-secondary: #5c4e3a;
  --color-text-muted: #746852;
  --color-brand: #8b6f3a;
  --color-brand-bright: #a78348;
  --color-brand-contrast: #fffaf0;
  --color-divider: rgb(74 58 30 / 18%);
  --color-divider-strong: rgb(74 58 30 / 38%);
  --color-focus: rgb(139 111 58 / 52%);
  --glass-blur: 16px;
  --image-hero: url("../assets/daowen-hero-day.png");
  --shadow-1: 0 8px 24px rgb(60 44 22 / 10%);
  --shadow-2: 0 24px 70px rgb(60 44 22 / 18%);
  --shadow-modal: 0 35px 100px rgb(60 44 22 / 24%);
}

[data-theme="noon"] {
  color-scheme: dark;
  --color-canvas: #6a471f;
  --color-surface: rgb(66 45 20 / 64%);
  --color-surface-soft: rgb(99 68 30 / 58%);
  --color-surface-strong: rgb(42 29 13 / 88%);
  --color-text: #fff6df;
  --color-text-secondary: #e7d2a5;
  --color-text-muted: #c3ad82;
  --color-brand: #f2c76c;
  --color-brand-bright: #ffe2a0;
  --color-brand-contrast: #2a1a08;
  --color-divider: rgb(255 218 143 / 28%);
  --color-divider-strong: rgb(255 226 164 / 54%);
  --color-focus: rgb(255 226 160 / 66%);
  --glass-blur: 20px;
  --image-hero: url("../assets/daowen-hero-noon.png");
}

body {
  color: var(--color-text);
  background-color: var(--color-canvas);
  font: 400 var(--text-md) / var(--leading-body) var(--font-body);
  transition:
    color var(--duration-theme) var(--ease-standard),
    background-color var(--duration-theme) var(--ease-standard);
}

.surface {
  color: var(--color-text);
  background: var(--color-surface);
  border: 1px solid var(--color-divider);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-1);
  backdrop-filter: blur(var(--glass-blur)) saturate(1.05);
}

.button-primary {
  min-height: var(--control-h);
  padding-inline: var(--space-6);
  color: var(--color-brand-contrast);
  background: linear-gradient(120deg, var(--color-brand), var(--color-brand-bright));
  border: 1px solid var(--color-divider-strong);
  border-radius: var(--radius-md);
  transition: transform var(--duration-fast) var(--ease-standard),
              box-shadow var(--duration-fast) var(--ease-standard);
}

.button-secondary {
  min-height: var(--control-h);
  padding-inline: var(--space-6);
  color: var(--color-text);
  background: transparent;
  border: 1px solid var(--color-divider-strong);
  border-radius: var(--radius-md);
}

:where(button, a, input, select, textarea):focus-visible {
  outline: 2px solid var(--color-focus);
  outline-offset: 2px;
}

@media (max-width: 760px) {
  :root { --control-h: var(--control-h-mobile); }
  .tool-modal {
    width: 100vw;
    height: 100dvh;
    max-width: none;
    max-height: none;
    border-radius: 0;
    overflow-y: auto;
  }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 1ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-duration: 1ms !important;
  }
}
```

## 11. 设计与开发验收清单

- 390px 宽度下导航没有半字截断，所有点击热区 ≥44px。
- 三主题切换不刷新页面、不关闭弹窗、不清空表单，500ms 内完成。
- 夜晚/白天/中午三主题正文对比度达到 WCAG AA。
- 全站正式 UI 中不存在系统 Emoji。
- 桌面弹窗只有内部滚动；移动弹窗铺满视口；底层页面始终锁定。
- 所有购买入口进入独立支付页，取消或完成后返回来源功能并恢复输入。
- AI 和结果正文不低于 16px，长文本行高不低于 1.75。
- 同级卡片使用统一圆角、边框和阴影；组件中不出现硬编码主题色。
- 支付加载中禁止重复提交，失败状态保留表单和来源页面状态。
- 键盘可访问所有按钮、Tab、关闭和导航；焦点样式清晰可见。
